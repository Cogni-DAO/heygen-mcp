optConfig:
  optIn: true         # required for single-repo install

action: fix           # actually set branch protections

enforceDefault: true
enforceOnAdmins: true
requireUpToDateBranch: true

requireApproval: true   # set true + approvalCount if you want reviews
approvalCount: 1
